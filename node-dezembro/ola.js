for (i = 0; i <= 100; i += 10){
    console.log(i);
};

function inverterTexto(texto) {
    return texto.split('')  // Divide a string em um array de caracteres
                .reverse()  // Inverte o array
                .join('');  // Junta os caracteres novamente em uma string
}

const textoOriginal = "Olá, Mundo!";
const textoInvertido = inverterTexto(textoOriginal);

console.log(textoInvertido);  // Saída: "!odnuM ,álO"


   let vogal = ["a","e","i","o","u"]
    for(let ini = 0;ini >= textoOriginal.length-1; ini++ ){
        let letra = text
    }
   
    var ord = textoOriginal.split("").sort().join();
    console.log(ord);


   


